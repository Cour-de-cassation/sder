import { settingsType } from '../../../modules/settings';
export { buildEntityIdMapper };
declare function buildEntityIdMapper(settings: settingsType, entityIds: string[], seed: number): Record<string, string>;
