import { normalize } from './normalize';
import { cleanXml } from './cleanXml';
import { xmlToJson } from './xmlToJson';

export { jurinetUtils };

const jurinetUtils = {
  normalize,
  cleanXml,
  xmlToJson,
};
